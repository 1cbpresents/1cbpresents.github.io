# Archive

This folder keeps legacy pages, old assets, and the previous Bootstrap example files that are no longer part of the active site.

The active public pages are kept at the repository root:

- `index.html`
- `datenschutz.html`
